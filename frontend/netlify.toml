[build]
  publish = "dist"
  command = "npm run build"

# SPA fallback — all unmatched routes serve index.html so React Router handles them
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
